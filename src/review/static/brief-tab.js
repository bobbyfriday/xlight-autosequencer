'use strict';

// brief-tab.js — Creative Brief form controller (spec 047).
//
// Mounts the Brief form into the per-song workspace shell (spec 046).
// See specs/047-creative-brief/ for the full contract.

(function () {
  // Skeleton — real implementation lands in Phase 3+.
  window.mountBriefTab = function mountBriefTab(root, sourceHash) {
    // No-op until Phase 3 implementation.
  };
})();
